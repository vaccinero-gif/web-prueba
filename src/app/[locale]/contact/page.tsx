'use client';import {useState} from 'react';
export default function Contact(){const [msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const form=new FormData(e.currentTarget);const body=Object.fromEntries(form.entries());const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});setMsg(r.ok?'Submitted successfully.':'Please complete all required fields.');}
return <div className='container-main'><form onSubmit={submit} className='card grid gap-3 max-w-xl'><h1 className='text-3xl font-semibold'>Contact</h1>{['name','email','nationality','city','topic'].map(f=><input key={f} required name={f} placeholder={f} className='border p-2 rounded'/>)}<textarea required name='message' placeholder='message' className='border p-2 rounded'/><button className='rounded bg-brand-600 px-4 py-2 text-white'>Send</button><p className='text-sm'>{msg}</p></form></div>}
