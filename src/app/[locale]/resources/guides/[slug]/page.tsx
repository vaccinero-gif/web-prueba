import {site} from '@/lib/site-config';
import {readContent} from '@/lib/site.server';import {notFound} from 'next/navigation';
export default async function Guide({params}:{params:{locale:string;slug:string}}){const d=await readContent<any>(params.locale,'site.json');const g=d.guides.find((x:any)=>x.slug===params.slug);if(!g)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:g.title,dateModified:g.updated,inLanguage:params.locale,author:{"@type":"Organization",name:site.name}};return <div className='container-main'><article className='card'><script type='application/ld+json' dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><h1 className='text-3xl font-semibold'>{g.title}</h1><p className='text-xs my-2'>Last updated: {g.updated}</p><p>{g.body}</p></article></div>}
