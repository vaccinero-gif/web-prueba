import Link from 'next/link';import {readContent} from '@/lib/site';
export default async function Resources({params}:{params:{locale:string}}){const d=await readContent<any>(params.locale,'site.json');return <div className='container-main'><h1 className='text-3xl font-semibold'>Resources</h1><div className='mt-6 grid gap-4'>{d.guides.map((g:any)=><article key={g.slug} className='card'><h2 className='font-semibold'>{g.title}</h2><p className='text-xs'>Last updated: {g.updated}</p><Link href={`/${params.locale}/resources/guides/${g.slug}`}>Read guide</Link></article>)}</div><div className='mt-6 flex gap-4'><Link href={`/${params.locale}/resources/faq`}>FAQ</Link><Link href={`/${params.locale}/resources/glossary`}>Glossary</Link></div></div>}
