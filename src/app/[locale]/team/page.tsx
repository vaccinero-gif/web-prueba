import {readContent} from '@/lib/site';
export default async function Team({params}:{params:{locale:string}}){const d=await readContent<any>(params.locale,'site.json');const schema=d.team.map((p:any)=>({"@context":"https://schema.org","@type":"Person",name:p.name,jobTitle:p.role,knowsLanguage:p.languages}));return <div className='container-main'><script type='application/ld+json' dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><h1 className='text-3xl font-semibold mb-6'>Team</h1><div className='grid gap-4 md:grid-cols-2'>{d.team.map((p:any)=><article key={p.name} className='card'><h2 className='font-semibold'>{p.name}</h2><p className='text-sm'>{p.role}</p><p className='mt-2 text-sm'>{p.bio}</p><p className='mt-2 text-xs'>Languages: {p.languages.join(', ')}</p></article>)}</div></div>}
